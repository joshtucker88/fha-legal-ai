VITE_CONVEX_URL=http://127.0.0.1:3210
VITE_CONVEX_SITE_URL=http://127.0.0.1:3211

# The RAG assistant needs an OpenAI key. Do NOT put it here or in any committed file.
# Set it as a Convex backend environment variable instead:
#   npx convex env set OPENAI_API_KEY sk-...
# Optionally override the generation model (defaults to gpt-4o-mini):
#   npx convex env set OPENAI_CHAT_MODEL gpt-4o
